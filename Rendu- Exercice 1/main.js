let Character = {
    name : "John",
    sanity : 10
}

let Trajet = { // ne faudrait-il pas une classe à la place ?
    radio : "La radio",
    lights : 30,
    swap : 0
}

musics = ["one", "two", "three", "four", "Anissa - Wejdene"]  // comment on fait de la proba pour une liste... 


// Initialiser un compteur de 30 "Feux Rouges" qui prendra -1 à chaque fois
// swap aussi est basiquement un compteur